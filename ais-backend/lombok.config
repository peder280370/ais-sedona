config.stopBubbling = true
# Adds @ConstructorProperties to generated constructors so Jackson can deserialize
# without requiring explicit @JsonCreator / @JsonProperty annotations
lombok.anyConstructor.addConstructorProperties = true
